description: Returns a list of followers and a counter of followers
content:
  application/json:
    schema:
      type: object
      properties:
        followers:
          type: array
          items:
            $ref: ../../_index.yaml#/schemas/User
        count:
          type: integer
          example: 1
