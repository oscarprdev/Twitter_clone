type: object
properties:
  email:
    type: string
    format: email
    description: "The user's email address."
    nullable: false
    example: 'tommsun@email.com'
  password:
    type: string
    description: "The user's password."
    nullable: false
    example: '1234'
required:
  - email
  - password
